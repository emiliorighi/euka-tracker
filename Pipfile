[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
ete3 = ">=3.1.0"
requests = ">=2.31.0"
biopython = ">=1.83"
pandas = ">=2.1.0"
shapely = ">=2.0.0"
pyarrow = ">=14.0.0"
numpy = ">=1.24.0"
umap-learn = ">=0.5.0"
xmltodict = ">=0.13.0"
ratelimit = ">=2.2.1"
tenacity = ">=8.2.3"

[dev-packages]

[requires]
python_version = "3.12"
